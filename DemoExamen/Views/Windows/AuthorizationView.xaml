<Window x:Class="DemoExamen.Views.AuthorizationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoExamen.Views"
        
        mc:Ignorable="d"
        Title="AuthorizationView" Height="450" Width="800">
    <Grid>
        <StackPanel Width="240" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock TextAlignment="Center" FontSize="20">
                Авторизация
            </TextBlock>
            
            <Label>Логин:</Label>
            <TextBox Text="{Binding Login}"/>
            <Label>Пароль:</Label>
            <PasswordBox x:Name="passwordBox"/>

            <Button Margin="0 20 0 0" Command="{Binding Enter}">
                Войти
            </Button>
            
        </StackPanel>
    </Grid>
</Window>
