<Window x:Class="DemoExamen.Views.Windows.ResetPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoExamen.Views.Windows"
        mc:Ignorable="d"
        Title="ResetPasswordView" Height="350" Width="600" WindowStartupLocation="CenterScreen">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label>Текущий пароль</Label>
        <PasswordBox x:Name="CurrentPassword"/>
        <Label>Новый пароль</Label>
        <PasswordBox x:Name="NewPassword"/>
        <Label>Подтверждение проля</Label>
        <PasswordBox x:Name="ConfirmPasssword"/>
        <Button Content="Изменить пароль" 
                Command="{Binding ChangePassword}"/>
    </StackPanel>
</Window>
